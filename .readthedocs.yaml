# .readthedocs.yaml

version: 2

# Required
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

# Optional but recommended
python:
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
  system_packages: false

sphinx:
  configuration: docs/conf.py

# Optional: for installing extras
# python:
#   install:
#     - method: pip
#       path: .
#       extra_requirements:
#         - docs

# Optional: custom build output
# build:
#   output_base: _build

# Optional: environment variables
# environment:
#   variables:
#     YOUR_ENV_VAR: "value"
